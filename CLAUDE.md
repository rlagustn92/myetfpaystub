# MY ETF 급여명세서 — 작업 규칙

여러 증권사에 흩어진 내 ETF 를 한곳에 모아, **매달 받는 돈**과 **세금 계산 기준**을
쉽게 확인하는 개인 투자 관리 도구. 한국어 Streamlit 앱.

- 배포 예정 주소: `MYETFPAYSTUB` (Streamlit Community Cloud)
- 데이터 소스 검증 기록: **[docs/DATA_SOURCES.md](docs/DATA_SOURCES.md)** — 코드를 깊게
  건드리기 전에 §1(분배금 ≠ 과세표준)과 §2-5(SOL 필드 함정)를 먼저 읽으세요.

---

## 대원칙 — 금융의 벽을 넘겨주는 엘리베이터

**보는 사람은 금융 초보라고 가정합니다.** 과세표준이 뭔지, 배당락이 뭔지 모르는 사람이
아무 설명 없이 들어와서, 배우지 않고도 "내 ETF 가 얼마이고 이번 달에 얼마 들어오는지"
를 알고 나가야 합니다. 사용자를 공부시키지 않습니다.

그래서 이렇게 씁니다.

| 전문용어 | 기본 화면에서 쓰는 말 |
|---|---|
| 평가금액 | 지금 내 돈 / 지금 내 ETF 자산 |
| 평가손익 | 지금까지 벌거나 잃은 돈 |
| 분배금 | ETF 월급 |
| 과세표준 | 세금 계산에 잡히는 금액 |
| 원천징수세액 | 세금으로 빠진 돈 |
| 증권사 | 어디에 가지고 있나요? |
| 계좌 | 어떤 계좌인가요? |

상세 화면에서는 `주당 과세표준액` 처럼 원래 용어를 병기해도 됩니다.
**숫자 하나만 던지지 않습니다** — 항상 바로 아래 한 줄로 그게 무슨 뜻인지 적습니다.

새 기능을 만들 때 자문할 것: **"금융 모르는 사람이 이 화면만 보고 이해할까?"**
아니라면 기능이 아니라 문구가 잘못된 것입니다.

---

## 절대 규칙

1. **데이터를 지어내지 않는다.** 값이 없으면 화면에 `데이터 없음` 이라고 적습니다.
   - `0` 과 `None` 을 섞지 않습니다. 과세표준 `0원` 은 **운용사가 발표한 값**이고,
     `None` 은 **모른다**는 뜻입니다. 코드에서도 화면에서도 구분합니다.
   - 합계를 낼 때 모르는 값을 0 으로 세지 않습니다. 빼고 세고, **뺐다고 화면에 적습니다.**
     ⚠ **예외 하나 — 과세표준 미발표는 0원으로 셉니다**(2026-09-21 사용자 결정).
     표 안에 "운용사 미발표" 같은 긴 글이 섞이면 지저분하고, 셀의 숫자와 합계가
     안 맞는 것도 이상해서입니다. 대신 셀에 `0원*` 처럼 별표를 달고 **표 아래
     한 줄**로 "미발표 N건 — 0원으로 계산했습니다" 를 적습니다. 정보를 숨기는
     게 아니라 **자리를 옮긴 것**입니다.
   - 추정이면 `🟡 예상` 을 붙이고 근거를 같이 보여줍니다. 다음 달 값을 확정값처럼
     보여주는 일은 절대 없습니다.
2. **분배금에서 과세표준을 유도하지 않는다.** 반드시 운용사가 발표한 별도 필드를 씁니다.
   같은 종목이 분배금 300원 / 과세표준 2원인 달이 실제로 있습니다.
3. **세금은 계좌 유형에 따라 갈라서 보여준다.** (2026-09-21 사용자 결정으로 개정)
   - **ISA · 연금저축 · IRP** — 지급할 때 세금을 **안 뗍니다**(과세이연).
     들어오는 그대로 입금됩니다. 최종 세금은 나중에 찾을 때 개인 상황
     (가입기간·나이·한도)에 따라 정해지므로 **계산하지 않고 그 사실만 알립니다.**
   - **일반(위탁) 계좌** — 과세표준액 + `과세표준 × 15.4%` 예상 세금 + 세후 금액을
     보여줍니다. 내 수량으로 대략 얼마인지가 제일 궁금한 부분이라서요.
     ⚠ **예상**이라고 반드시 적습니다. 금융소득종합과세나 외국납부세액이
     얽히면 달라집니다. 실제 뗀 금액은 증권사 내역이 맞습니다.
   - 세율은 `config.WITHHOLDING_RATE`(0.154 = 소득세 14% + 지방세 1.4%) 한 곳에만
     둡니다. 계좌 판별은 `config.TAX_DEFERRED_ACCOUNT_WORDS`.
   - ⭐ **세율은 분배금이 아니라 과세표준액에 곱합니다.** 같은 종목이 분배금
     150,000원 / 과세표준 1,000원인 달이 실제로 있어서, 분배금에 곱하면 세금이
     150배 부풀어 오릅니다. 테스트가 막고 있습니다.
4. **배포는 사용자가 승인한다.** 커밋까지는 해도, "이 버전 배포할까요?" 를 묻고
   답을 받은 뒤에 push 합니다.
5. **사용자에게 보이는 변화가 있으면 `config.APP_VERSION` 을 올린다.** 아주 작은
   수정이라도 올립니다. 화면 헤더의 서비스 이름 옆에 그대로 표시됩니다.
6. **개인정보를 받지 않는다.** 주민번호·계좌번호·증권사 로그인정보·API secret 은
   입력칸조차 만들지 않습니다. 투자 정보는 브라우저 저장소와 JSON 파일에만 남습니다.
7. **다른 프로젝트 폴더를 고치지 않는다.** 데스크톱의 `ETF MANAGER 2027` 등은
   참고 전용입니다. 코드를 재사용할 땐 이 저장소로 복사해 와서 고칩니다.

---

## 실행과 테스트

```bash
python -m pytest -q          # 전체 (네트워크 테스트는 자동 제외)
python -m pytest -m network  # 실제 운용사/시세 호출 (20초쯤)
streamlit run app.py         # 로컬 실행
```

기능을 하나 만들 때마다 테스트도 같이 만들고 돌립니다. 몰아서 하지 않습니다.
**항상 건너뛰는(skip) 테스트는 아무것도 검증하지 않습니다.**

`-m network` 는 배포 전에 한 번씩 돌리세요. 단위 테스트는 저장해 둔 응답 조각으로만
검증하므로, 운용사가 필드 이름을 바꿔도 조용히 통과합니다. 그걸 잡는 게 network 테스트입니다.

---

## 꼭 알아야 할 함정

- **`app.py` 만 핫리로드됩니다.** `services/`, `data/`, `config.py`, `models/`,
  `components/` 를 고치면 서버 재시작(클라우드는 Reboot 또는 새 커밋 push)이 필요합니다.
  이걸 모르고 "안 바뀌는데?" 로 시간을 날리기 쉽습니다.
- **SOL 의 `TAX_PRI` 는 과세표준이 아닙니다.** 과세기준가격(9,102원 같은 큰 수)입니다.
  주당 과세표준액은 **`WEEK_PRI`** 입니다. 잘못 쓰면 에러 없이 숫자가 45배 틀립니다.
- **TIGER 는 `listCnt` 를 크게 보내도 20건만 줍니다.** 페이징을 안 하면 그 달 상위
  20종목만 들어오고 나머지는 조용히 "분배 이력 없음"이 됩니다. 실제로 당했습니다.
- **TIGER 는 `selectMonth` 를 비우면 그 해 합계만 오고 날짜가 빕니다.** 개별 지급 건을
  받으려면 반드시 월을 지정해야 합니다. 그래서 (연,월) 단위로 받아 캐시합니다.
- **한국 종목코드는 6자리 문자열이고 영문이 섞일 수 있습니다** (`0219E0`).
  `zfill(6)` 로 앞의 0 을 살리고, `.KS/.KQ` 접미사는 `isdigit()` 이 아니라
  **길이가 6이면** 붙입니다.
- **위젯이 만들어진 뒤에는 그 키의 `session_state` 를 못 바꿉니다.**
  `StreamlitWidgetAlreadyInstantiatedError` 가 납니다. 종목 추가 칸에서 한 번
  겪고 적어 뒀는데 **목표 입력칸에서 또 밟았습니다**("목표 지우기" 가 터짐).
  값을 비우려면 **깃발만 세우고**(`goal_clear`, `add_reset`) 실제로 비우는 일은
  **다음 실행에서 위젯을 만들기 전에** 합니다.
- **브라우저 저장소는 화면 맨 위에서 읽고 맨 아래에서 씁니다.** 읽기를 아래로 내리면
  `StreamlitWidgetAlreadyInstantiatedError`, 쓰기를 위로 올리면 이번에 고친 내용이 안 남습니다.
- **'🔄 정보 업데이트' 는 관리자만 누릅니다** (`app.is_admin()`, 주소에
  `?admin=<키>`). 캐시는 **모든 접속자가 함께 쓰기 때문에** 한 번의 클릭이
  전체 종목을 다시 받아오게 만듭니다. secrets 의 `ADMIN_KEY` 가 없으면 항상
  관리자입니다(개인 PC 용). **키를 코드에 적지 마세요 — 저장소가 공개입니다.**
- **TIGER 의 지나간 달은 `data/tiger_months.csv` 에서 읽습니다(호출 0회).**
  캐시는 **서버 메모리**뿐이라 재시작하면 날아가고, 무료 호스팅은 접속이 없으면
  앱을 재웁니다 — 그래서 **깨어날 때마다 43회**였습니다. 메모리 캐시로는 못 막고,
  `issuer_index.csv` 처럼 **저장소 파일**로 굳혀야 영구적입니다(배포 때 같이
  복사됩니다). 실측: 캐시가 빈 상태에서 43회 -> **1회**(이번 달만).
  ⭐ 월별 표는 **종목이 아니라 달에 붙습니다.** 한 달치에 그 달 TIGER 전 종목이
    들어 있어서, 달만 굳히면 종목이 몇 개든 파일이 안 커집니다.
  ⚠ **아직 안 끝난 달은 파일에 넣지 않습니다.** 넣으면 앱이 최종본으로 믿고
    나중에 발표된 과세표준을 영영 못 봅니다. 빌더가 걸러냅니다.
  ⚠ 파일을 쓸지 정하는 규칙과 캐시를 오래 둘지 정하는 규칙은 **같아야** 합니다
    (`_month_is_settled` 가 `_month_ttl` 하나를 봅니다). 따로 두면 조용히 어긋납니다.
  갱신: `python tools/build_tiger_months.py` — **개발자 PC 에서만**. 덮어쓰지 않고
  없는 달만 더하므로 돌릴수록 이력이 길어지고 실행 비용은 줄어듭니다.
- **밖에 나간 횟수를 셉니다**(`data/providers/callmeter.py`, 운용사는
  `issuer/base.py`). 관리자 화면 아래 "오늘 조회 — 운용사 N회 · 시세 N회 ·
  환율 N회". **캐시에서 꺼내 쓴 건 안 셉니다** — 그래서 이 숫자가 곧
  "캐시가 잘 듣고 있나" 입니다. 세는 자리는 반드시 `_load()` **안쪽**이어야
  합니다(밖으로 한 줄 옮기면 적중까지 세는데 에러가 안 나서 모릅니다).
  한도(`PRICE_DAILY_CALL_BUDGET`, 기본 600)는 평소에 닿을 숫자가 아니라
  **코드 실수로 폭주하는 것을 끊는 안전장치**입니다.
- **장이 닫혀 있으면 최신가를 다시 안 받습니다**(`price_provider.latest_price_ttl`).
  **장중 15분 / 그 외 6시간**이고, ⭐ **시장마다 따로 봅니다** — 한국과 미국은
  열리는 시간이 정반대라 시계 하나로 보면 한쪽은 반드시 틀립니다.
  한국 종목은 09:00~15:40, 미국 종목은 22:00~06:30(한국시간)에만 15분입니다. 밤과 주말에는 종가가 **아예 안 바뀌는데**
  30분마다 다시 받고 있었습니다 — 실측으로 한 주의 **79%(한국) · 73%(미국)** 가
  장이 닫힌 시간입니다. 최악의 경우 한 종목에 하루 96회 -> **16회**.
  ⚠ 유효기간은 **읽을 때마다** 다시 정합니다(`cache.get_or_set(ttl_of=...)`).
    저장할 때 고정하면 장 마감 직전에 받은 값이 밤새 30분마다 다시 받아집니다.
  ⚠ 미국장은 한국 시간으로 **밤에 열려 다음 날 새벽에 닫힙니다.** 요일을 그냥
    보면 금요일 밤 장(= 토요일 새벽)을 닫힌 것으로 칩니다.
  ⚠ 두 시장의 15분 구간은 **절대 안 겹칩니다**(테스트가 24시간을 훑어 확인).
    겹치게 나오면 시간 창을 잘못 잡은 것입니다.
  ⚠ **캐시가 비어 있으면 시간과 상관없이 한 번은 받아옵니다.** 안 그러면
    새벽에 들어온 사람에게 자산이 통째로 "데이터 없음" 이 됩니다.
  ⚠ 공휴일은 모릅니다. 틀리면 호출이 몇 번 더 나갈 뿐 값은 안 틀립니다 —
    시간 창을 **넓은 쪽으로** 잡아 두었습니다(가격이 멈춰 보이는 게 더 나쁩니다).
- **끝난 달의 분배금은 다시 안 받습니다.** 지나간 달 자료는 안 바뀌는데 TTL 이
  하나뿐이라 몇 년 전 것까지 하루마다 다시 받고 있었습니다 — TIGER 는 달마다
  따로 불러야 해서 한 종목에 **HTTP 43번**이 나갔습니다(자료는 4건인데).
  `cache.get_or_set(..., ttl_of=...)` 로 **저장된 값을 보고** 유효기간을 정합니다:
  그 달이 지났고 + 과세표준 빈 칸이 없으면 30일, 아니면 24시간
  (`tiger_provider._month_ttl`). 실측 43회 -> 1회.
  ⚠ 과세표준은 **나중에 채워지는 일이 있어서**(2026-05·06 이 몇 달째 빈 칸)
    빈 칸이 남은 달은 계속 확인해야 합니다. 그냥 "지나간 달 = 영구" 로 두면
    나중에 발표된 값이 영영 안 보입니다.
- **월말이 기준인 ETF 는 돈이 다음 달 초에 들어옵니다.** 기준일 8/31 → 실지급
  9/2. RISE 200위클리커버드콜은 31건이 **전부** 이렇고 TIGER·KODEX 의
  미국S&P500 류도 같습니다. 달은 **실지급일**로 묶습니다("이번 달에 얼마
  들어오나" 가 질문이라서요). 대신 달이 갈리는 건에는 기준일을 같이 적습니다
  (`PayslipRow.record_note`) — 안 적으면 "8월분인데 왜 9월에?" 가 됩니다.
- **운용사 호출에는 하루 한도가 있습니다**(`ISSUER_DAILY_CALL_BUDGET`, 기본 600).
  `issuer/base.py` 의 `http_get`·`http_post` 가 부르기 **전에** 셉니다.
  삼성자산운용 API 가 한동안 `{"dividList":[],"totalCnt":0}` 만 돌려준 적이
  있습니다 — HTTP 200 에 정상 JSON 이라 **에러가 안 나고** 조용히 폴백으로
  내려가 과세표준이 전부 사라집니다. 남이 막기 전에 우리가 먼저 멈춥니다.
- **배당금에는 `(세전)` 을 붙입니다**(`config.PRETAX_SUFFIX`). 안 적으면
  통장에 꽂히는 돈으로 오해합니다. 세금을 뗀 값은 `세후 예상 입금액`.
- **"예상 세금" 이 아니라 `예상 원천징수액`** 입니다(`WITHHOLDING_LABEL`).
  무엇에서 떼는 돈인지가 이름에 있어야 증권사 내역과 말이 맞습니다.
  ⚠ **금액 앞에 마이너스를 붙이지 않습니다.** 라벨이 이미 "떼 가는 돈" 이라고
    말하고 있어서, 부호까지 붙이면 잃은 돈처럼 보입니다(사용자 지적).
- **배당률은 두 개고, 기준이 다릅니다**(`cashflow_service.ratio_of_cost` /
  `yield_on_cost`). 금액 옆 알약은 **이번 달 금액 ÷ 원금**(한 달치,
  "원금의 0.51%"), 카드 아래 칸은 **최근 1년에 실제로 받은 돈 ÷ 지금
  원금**("원금 대비 (최근 1년) 연 6.1%") 입니다.
  ⚠ 알약에는 **그 큰 금액에서 바로 나온 값만** 넣습니다. 연 기준을 옆에
    붙이면 바로 옆 금액과 계산이 안 맞아 "이 숫자 뭐지?" 가 됩니다.
  ⚠ **예상값은 안 셉니다.** 커버드콜은 달마다 배당이 널뛰어서 예상 한 건이
    숫자를 통째로 흔듭니다. "이번 달 × 12" 연환산을 안 쓰는 이유도 같습니다.
  ⚠ 분모는 **지금 원금**이라 최근에 많이 산 사람은 낮게 나옵니다. 맞는
    동작이므로 화면에 "최근 1년에 받은 돈 ÷ 지금 투자 원금" 이라고 적습니다.
  ⚠ 원금을 모르면 `pct` 는 **`None`** 입니다. 0% 로 적으면 거짓말입니다.
- **값 카드의 큰 금액은 칸 폭에 맞춰 줄어듭니다**(`ui.fit_style`). 평가손익은
  사람에 따라 몇천만원·몇억이 되므로 크기를 고정하면 언젠가 반드시 넘칩니다.
  넘치면 "+₩416,675" 가 **부호에서 잘려 "+" 만 윗줄에 남습니다**(실제로 당했습니다).
  CSS 는 글자 수를 못 세므로 파이썬이 세어 `calc((100cqi - 36px) * 1.72 / N)`
  으로 넘깁니다. 한글은 숫자보다 넓어서 1.9배로 셉니다.
  ⚠ `cqi` 는 `.kcard { container-type:inline-size }` 가 있어야 동작합니다.
    그 한 줄을 지우면 **조용히** 안 먹습니다(테스트가 지킵니다).
  ⚠ 그래도 한 줄에 카드를 여섯 장 넣지 마세요. 글자가 12px 까지 작아져서
    라벨과 크기가 비슷해집니다. 종목별 상세는 **3 + 3** 입니다.
- **Streamlit 1.63 의 셀렉트박스는 BaseWeb 이 아니라 react-aria 입니다.**
  `[data-baseweb="select"]` 로 색을 입히면 **아무 일도 안 일어납니다**(에러도
  안 납니다). 실제 DOM 은 `.stSelectbox .react-aria-ComboBox > [role="group"]`.
  위젯은 우리 HTML 로 감쌀 수 없으므로 `st.container(border=True, key=...)` 에
  `.st-key-<key>` 로 붙입니다 — key 와 CSS 가 문자열로만 묶여 있습니다.
- **투명도가 곧 대비입니다.** 월급 카드 라벨을 `opacity:.62` 로 뒀더니 흰
  글자인데도 대비가 4.0 이라 AA 미달이었습니다(.72 부터 4.5 를 넘습니다).
  은은하게 보이려다 안 읽히면 안 됩니다.
- **큰 숫자는 굵기가 아니라 크기와 여백으로 무게를 갖습니다.** 월급 금액을
  `font-weight:800` 으로 뒀더니 "무식하게 크고 두껍다" 는 지적을 받았습니다.
  500 으로 낮추고 크기를 키우니 훨씬 가볍고 우아해졌습니다.
- **목록의 손익은 한국 관습대로 오른 것이 빨강, 내린 것이 파랑**입니다
  (`row_html(sub_tone=...)`, `app._tone`). 회색으로 두면 플러스인지
  마이너스인지 눈으로 안 잡힙니다.
- **연간 배당금 목표는 사용자가 정합니다**(`Portfolio.dividend_goal_krw`).
  앱이 추천하거나 자동으로 잡지 않습니다 — 얼마를 목표로 할지는 투자 판단이라
  우리가 낄 자리가 아닙니다. 남은 금액을 "이만큼 더 사면 됩니다" 로 바꾸지도
  않습니다(매수 추천은 안 만들기로 한 것입니다).
  진행 막대는 **받은 돈(진한 색)과 들어올 돈(흐린 색)을 나눠** 그립니다.
  섞으면 "벌써 다 받은 것" 처럼 보입니다.
- **배당 성장은 과거만 말합니다**(`distribution_service.dividend_growth`).
  최근 1년과 그 전 1년의 **실제 지급액**을 나란히 놓을 뿐, 앞으로도 그만큼
  나온다는 뜻이 아니라고 화면에 적습니다.
  ⚠ 예상값(`STATUS_ESTIMATED`)은 안 셉니다 — 늘어난 게 예상 때문인지 실제
    때문인지 알 수 없게 됩니다.
  ⚠ 그 전 1년에 지급이 없으면 **비교하지 않습니다.** 0 으로 세면 성장률이
    무한대로 나옵니다(상장한 지 얼마 안 된 종목).
- **붙여넣기로 같은 종목을 다시 넣을 때 수량을 더하면, 평단가는 수량으로
  가중평균합니다.** 새 값으로 그냥 덮으면 원금이 틀어집니다.
- **월별 막대에 작년을 겹칠 때 높이는 두 해를 합쳐 잽니다.** 올해만 보고
  재면 작년 막대가 칸을 뚫고 나갑니다.
- **분배금은 종목별로 나눠 받습니다**(`distribution_service.fetch_all`,
  스레드 4개). 차례로 기다리면 첫 화면이 종목 수에 비례해 느려집니다
  (실측 5종목 3.55초 → 2.07초). ⚠ `MAX_WORKERS` 를 **올리지 마세요** —
  빨라지자고 운용사 서버를 한꺼번에 두드리면 막히고, 그러면 과세표준이
  통째로 사라집니다. 종목 하나가 터져도 나머지는 살아야 하므로
  `_fetch_one` 은 **예외를 밖으로 내보내지 않습니다**(테스트가 지킵니다).
- **방문자 카운터는 세션당 한 번만 부릅니다.** 안 그러면 클릭할 때마다 +1 입니다.
  `st.session_state["visitor_counts"]` 가 막고 있습니다.
- **KIWOOM(키움)은 과세표준을 공개하지만 붙이지 못했습니다.** 그 서버가 TLS 중간
  인증서를 안 보내서 파이썬에서 검증이 실패합니다. `verify=False` 로 끄지 마세요.
  자세한 건 docs/DATA_SOURCES.md §2-8.
- **`pitch_service.tidy()`(⚽ 포지션 자동 정리)는 버튼에서만 부릅니다.** 화면을
  열 때 자동으로 부르면 사용자가 손으로 끌어다 맞춘 배치가 말없이 흐트러집니다.
  화면을 열 때 도는 것은 `ensure_slots`(자리 **없는** 종목만 채움)입니다.
- **전술판 자리는 보유 줄이 아니라 종목(티커)에 붙습니다.** 같은 ETF 를 세 계좌에
  나눠 가져도 카드는 하나만 섭니다. `Portfolio.slots` 의 열쇠는 `"KR:069500"` 형태.
- **화면과 📸 캡처는 여백을 쌓는 방식이 달라서, 같은 간격을 내려면 숫자가 달라집니다.**
  화면은 `padding + 줄상자`, 캡처는 `26px 바 한가운데에 글자`입니다. "🏠 우리 진영 ·
  골키퍼 ▼" 가 **캡처에서만** 광고보드를 파고들었던 이유입니다(화면은 멀쩡했습니다).
  지금은 `#botbar { padding-top: 6px }` 과 `BOT_GAP = 10` 이 짝입니다 — 한쪽을
  고치면 **반드시 띄워서 다시 재세요.** 숫자를 맞추면 오히려 어긋납니다.
- **📸 캡처 이미지와 📋 텍스트는 같은 줄에서 만듭니다**(`pitch_service.share_rows`).
  따로 만들면 같은 포트폴리오를 두 군데 올렸을 때 숫자가 어긋나 보입니다.
- **전술판 카드의 등번호는 실제 보유 비중입니다.** 원본(ETF MANAGER)은 사용자가
  입력한 목표비중이었습니다. 여기서는 수량 × 현재가로 저절로 정해집니다.
- **`pitch_service._ROW_BY_KEYWORD` 는 순서가 중요합니다.** "커버드콜" 이 "200"
  보다 앞에 있어야 "KODEX 200타겟위클리커버드콜" 이 제 라인으로 갑니다.
- **반바지·양말에는 운용사 색이 한 톨도 들어가면 안 됩니다.** 처음에 테두리를
  `kit.dark`(운용사 색)로, 양말 띠를 몸통색으로 그렸더니 카드마다 하의가 달라
  "축구팀 같지 않다" 는 지적을 받았습니다. 하의 색은 `skins.py` 의
  `shorts`/`socks`/`sock_band` 만 씁니다. 테두리는 밝기로 정합니다(`outlineFor`).
- **반바지·양말 색은 실제 그 팀 조합을 씁니다.** 소매 색·깃 색에서 기계적으로
  파생시키면 조합이 엉터리가 되어 팀이 안 떠오릅니다(상의-반바지-양말 3색으로
  알아보기 때문입니다).
- **배너 글자색을 스킨 색에서 고르지 마세요.** `accent` 를 썼다가 광고보드와 같은
  값인 스킨 4벌에서 글자가 안 보였고, 단순 밝기 문턱값으로 바꿨더니 중간 밝기
  색에서 또 틀렸습니다(하늘색에 흰 글자 → 대비 2.98). 지금은 **흑·백 중 실제
  대비가 높은 쪽**을 고릅니다(`textOn`). 테스트가 21벌 전부 WCAG AA 를 검사합니다.
- **센터서클 배지는 지명 약자만** 씁니다. 구단 약칭(LFC·PSG·BVB 류)은 상표입니다.
- **불러오기는 "우리 파일처럼 생겼는지" 를 먼저 봅니다**(`storage_service.loads`).
  예전에는 `schema` 키가 **없으면** 그냥 통과시켜서, 아무 JSON 이나 넣어도
  성공으로 받고 **빈 포트폴리오**를 돌려줬습니다. 그 빈 내용이 화면 맨 아래에서
  브라우저 저장소에 덮여 써지므로 **등록해 둔 ETF 가 전부 조용히 사라졌습니다.**
  옛 저장본에는 `schema` 가 없을 수 있어 아는 키 하나만 있으면 받습니다.
- **평단가 입력은 못 읽으면 `None` 입니다. 0 이 아닙니다**(`app._parse_money`).
  "1.2.3" 같은 오타가 아무 말 없이 **평단가 0원**으로 저장되던 적이 있습니다.
  그러면 원금이 0 이 되고 수익률이 터무니없이 나옵니다. 저장 버튼을 막습니다.
  "3만"·"1억" 같은 한국식 단위도 읽습니다("3만5천" 처럼 섞이면 안 읽습니다 —
  틀린 숫자보다 다시 묻는 편이 낫습니다).
  ⚠ **보여주는 글자는 반드시 `_parse_money` 의 결과로 만듭니다**(`_format_money`).
    따로 다듬었다가 "3만" 을 30,000 으로 읽어놓고 화면엔 "3" 을 찍은 적이 있습니다.
- **원화 평단가에는 소수점을 쓰지 않습니다.** 1원 미만이 실제로 없습니다.
  편집 표의 `format="%.4f"` 때문에 "32000.0000" 으로 보였습니다.
- **붙여넣기에서 종목명은 안 씁니다 — 종목코드로 공식 이름을 찾습니다**
  (`search_service.resolve()`). 이름은 증권사마다 띄어쓰기도 줄임말도 달라
  사람들이 대부분 틀리게 적습니다(사용자 지적). 그대로 저장하면 같은 종목이
  계좌마다 다른 이름으로 남습니다. **미국은 티커가 종목코드 자리**입니다.
  ⚠ `resolve()` 는 **정확히 일치**하는 것만 돌려줍니다 — 검색처럼 비슷한 것을
    고르면 엉뚱한 종목이 등록되어 자산이 통째로 틀어집니다.
  ⚠ 미리보기에서도 부르므로 **네트워크를 타면 안 됩니다.** 시드에 없는 미국
    티커만 등록 단추를 누른 뒤 `resolve_us_ticker()` 로 확인합니다.
- **붙여넣기로 들어온 글에서 계좌번호를 먼저 버립니다**(`import_service`).
  증권사 화면을 긁으면 계좌번호가 딸려옵니다 — 절대규칙 6 이라 **읽는 단계에서**
  지웁니다. 저장은커녕 화면에도 안 올립니다.
- **표를 칸으로 쪼갤 때 빈 칸을 버리면 안 됩니다.** 탭/쉼표로 나뉜 표에서 빈 칸
  하나를 지우면 그 뒤 열이 전부 밀려 **수량 자리에 단가가 들어옵니다.** 에러가
  안 나고 자산만 엉뚱해집니다. 따옴표 안의 쉼표도 안 쪼갭니다(`"₩32,000"`).
- **머리글이 없는 줄은 종목코드를 먼저 찾습니다.** 왼쪽부터 훑으면
  "KODEX 200 069500 5000 10" 에서 KODEX 를 미국 티커로, 200 을 수량으로
  읽습니다(실제로 그랬습니다). 코드를 찾고 나면 앞은 이름, 뒤는 숫자입니다.
- **응원 배너는 위아래 둘 다 응원 문구**입니다(`chant` / `chant_home`). 아래쪽에
  서비스 이름을 적었더니 경기장 느낌이 깨졌습니다.
- **응원 배너는 그 나라 말**로 씁니다(`Skin.chant`). 영어 스킨 이름을 적었더니
  팀이 전혀 연상되지 않았습니다. ⚠ 지명 + 일반 응원어만 — **구단 공식 슬로건·
  응원가 제목·별칭은 상표/저작물**이라 씁니다(테스트가 막고 있습니다).
- **기본 스킨(classic)은 유니폼 몸통을 안 바꿉니다 — 여기가 정보의 기준선입니다.**
  `Skin.body` 를 **비워 두면** 몸통색 = 운용사 브랜드로 그려집니다(예전과 동일).
  아무 스킨도 안 고른 사람에게는 정보가 그대로 남아야 하기 때문입니다.
- **클럽풍 스킨은 몸통까지 팀 색으로 칠합니다.** 소매·깃·무늬만 물들여서는
  몸통 면적(유니폼의 70%)을 못 이겨서 팀이 전혀 안 떠올랐습니다("마드리드인지
  잘 모르겠다"). 몸통을 내주는 대신 **운용사는 이름표 배경 · 상의 테두리 ·
  가슴 두 글자** 세 곳에서 계속 보입니다. 시장 구분(태극/성조기)은 **가슴 문장**이
  지고 있어서 몸통색이 바뀌어도 안 깨집니다.
- **이름표 색은 운용사입니다 — 두 운용사가 비슷한 색이면 티가 안 나게 망가집니다.**
  미국 종목 이름표를 남색(#0A2B5C)으로 뒀다가 KODEX 남색(#0A1660)과 헷갈린다는
  지적을 받았습니다(색 거리 21). 지금 미국은 **성조기 빨강 #B31942 + 흰 테두리**
  입니다. 전수 점검에서 키움↔ACE(21), BNK↔TIGER(24)도 같은 문제라 짙은색만
  옮겼습니다. `tests/test_pitch_kit.py` 가 모든 짝의 색 거리와 글자 대비를 잽니다.
- **이름표 흰 테두리는 `outline` 입니다. `border` 로 바꾸지 마세요.** border 는
  이름표 높이를 키워서 카드 겹침 계산(`tests/test_pitch_layout.py`)을 흔듭니다.
  처음에는 아래쪽 흰 "띠" 로 넣었는데 흰 글자의 받침·꼬리가 띠를 파고들어
  글자가 잘려 보였습니다(글리프 아래끝 13.7px, 띠 시작 12.3px).
- **센터서클 배지는 국기·지역기 색띠 + 지명 약자**입니다(`badge_bars`/`badge_dir`).
  국기는 공공 상징이라 안전합니다. **구단 엠블럼은 물론이고 엠블럼을 연상시키는
  도형도 그리지 않습니다.** 도시 상징 도형(곰·벌·화산 …)을 시안으로 냈지만
  사용자가 "카탈루냐 깃발만 예쁘다" 고 해서 깃발로 정했습니다.
- **유니폼 무늬·센터서클 깃발은 화면과 캔버스가 같은 도형이어야 합니다.**
  무늬는 `patternSVG` ↔ `drawPattern`, 깃발은 **`badgeParts()` 한 함수**를
  양쪽이 같이 씁니다(깃발은 그래서 어긋날 수 없습니다). 무늬 좌표는 여전히
  두 곳에 적혀 있으니 같이 고쳐야 합니다.
  어긋나면 화면과 📸 캡처 이미지의 유니폼이 달라 보입니다. 잔디 깎기 무늬도 같습니다
  (`applySkin` ↔ 캔버스의 `mow` 분기).
- **등번호에는 몸통색 후광(stroke)을 두릅니다.** 줄무늬 위에 숫자가 걸치면 안 읽혀서요.
- **카드 높이·줄 간격·배너 두께는 서로 묶여 있습니다.** 하나만 건드리면 카드가
  겹치는데, 에러가 안 나고 화면만 지저분해져서 눈으로 볼 때까지 모릅니다.
  `tests/test_pitch_layout.py` 가 실제 CSS/JS 값을 읽어 317~900px 폭에서 검사합니다.
  숫자를 테스트에 베껴 적지 마세요 — 코드만 바뀌고 테스트는 통과하게 됩니다.
- **`pitch_grid.ROW_Y` 는 고르게 벌려야 합니다.** 들쭉날쭉하면 가장 좁은 칸부터
  겹칩니다(예전 값에서 모바일 간격이 5px 까지 좁아졌습니다).
- **`.card .shirt` 폭과 캔버스 `jerseyW` 는 같은 값이어야 합니다.** 다르면 공유한
  그림만 유니폼 크기가 다릅니다. 테스트가 둘을 비교합니다.
- **첫 렌더 때 판 폭이 0 일 수 있습니다.** 그때 계산한 글씨 크기가 그대로 굳어서
  배너가 16px 로 남았던 적이 있습니다. `ResizeObserver` 가 폭이 정해지는 순간
  다시 계산합니다 — 지우지 마세요.
- **스킨 색은 CSS 변수 한 벌로만 넘깁니다.** 📸 캡처 이미지가 화면에서 계산된
  값을 그대로 읽어 그리기 때문에, 한 곳만 바꾸면 화면과 이미지가 저절로 같아집니다.
  따로 칠하면 둘이 어긋납니다.
- **`components/ui.py` 의 `note()` 는 마크다운이 아닙니다.** `**굵게**` 를 넣으면
  별표가 그대로 찍힙니다(실제로 당했습니다).
- **스킨은 지금 전부 열려 있습니다.** `tier` 필드는 있지만 `is_unlocked()` 가 항상
  True 입니다. 아직 없는 잠금을 "곧 열립니다" 로 보여주지 않습니다.
  수익화 배경은 docs/호스팅-수익화-검토.md.
- **날짜는 전부 `config.today_local()`** 을 씁니다. 서버가 UTC 라 `date.today()` 를 쓰면
  한국 시간 오전 9시에 날짜가 바뀝니다.

---

## 구조

```
app.py                     화면 전체 (여기만 핫리로드)
config.py                  APP_VERSION · 캐시 시간 · 한국시간 · 문구 상수
formatting.py              금액/날짜 표기 (won, won_short, native_amt …)

models/
  portfolio.py             Holding(증권사·계좌·종목·수량·평단) / Portfolio
  distribution.py          Distribution — 분배금과 과세표준은 별도 필드
  numbers.py               바깥에서 온 값을 계산에 넣기 전에 거르는 곳

services/                  순수 계산 (테스트는 주로 여기)
  portfolio_service.py     자산·원금·손익, 종목별/증권사별 묶기
  distribution_service.py  분배 이력 가져오기 + 다음 지급 예상
  cashflow_service.py      월별 급여명세서 · 올해 누적 · 달력
  fx_service.py            환율 규칙 (과거는 그때 환율, 현재는 최신 환율)
  storage_service.py       저장/불러오기 (포트폴리오 여러 개)
  search_service.py        종목 검색
  import_service.py        ⭐ 증권사 잔고 붙여넣기 -> 종목 목록 (진입장벽 낮추기)
  pitch_service.py         전술판 — 보유 비중을 등번호로, 자리 배치
  skin_service.py          경기장 스킨 고르기/해금 (⭐ 해금 조건은 is_unlocked 하나만)
  share_service.py         📸 이미지 명단 · 📋 텍스트 (같은 줄에서 만듭니다)
  naver_link_service.py    네이버 증권 바로가기
  visitor_service.py       TODAY / TOTAL

data/providers/
  base.py cache.py         인터페이스 · TTL 캐시
  price_provider.py        미국 yfinance / 한국 FinanceDataReader
  fx_provider.py           USD/KRW
  issuer/                  ⭐ 운용사별 분배금·과세표준
    kodex_provider.py  tiger_provider.py  ace_provider.py  rise_provider.py
    sol_provider.py    html_table_provider.py (PLUS·TIME)
    generic_provider.py    폴백 (분배금만, 과세표준 없음) + 미국
    index.py               종목코드 -> 운용사 내부 ID (시드 CSV)
    registry.py            ⭐ 소스 교체 지점. 위층은 여기만 부릅니다

data/issuer_index.csv      843건 매핑 시드 (tools/build_issuer_index.py 로 갱신)
data/tiger_months.csv      TIGER 월별 분배 표 (tools/build_tiger_months.py 로 갱신)
                           지나간 달은 여기서 읽어 호출 0회. 안 끝난 달은 안 넣습니다
components/
  ui.py                    카드·목록·달력 + CSS (색은 여기서만 정합니다)
  local_store/             브라우저 저장소 통로 (순수 HTML/JS, 빌드 단계 없음)
  football_pitch/          ⚽ 전술판 (ETF MANAGER 2027 에서 옮겨 옴, 순수 HTML/JS)
  pitch_grid.py            슬롯 격자 5칸 × 6라인 = 26자리 (ROW_Y 는 고르게!)
  pitch_kit.py             유니폼 색(운용사 브랜드) + 긴 한국 ETF 이름 축약
  skins.py                 스킨 21벌 (기본 + 클럽풍 20) — 순수 데이터
                           잔디/깎기무늬/광고보드/관중석 + 유니폼 몸통·소매·깃·무늬
                           + 센터서클 국기 색띠 (body 를 비우면 운용사 색)
tools/build_issuer_index.py  매핑 시드 배치 — 개발자 PC 에서만 실행
tools/build_tiger_months.py  TIGER 월별 표 시드 — 개발자 PC 에서만 실행
tests/                     pytest
```

**계산 로직은 UI 와 분리합니다.** `app.py` 에서 금액을 직접 계산하지 마세요.

### 어디를 고치면 되나

| 바꾸고 싶은 것 | 파일 |
|---|---|
| 버전·캐시 시간·기본 증권사 목록·문구 | `config.py` |
| 금액 표기 | `formatting.py` |
| 자산/손익 계산 | `services/portfolio_service.py` |
| 예상 로직 | `services/distribution_service.py` |
| 월별 명세서·달력 | `services/cashflow_service.py` |
| 운용사 추가 | `data/providers/issuer/` 에 파일 추가 + `registry.KR_ISSUERS` 에 한 줄 |
| 시세 소스 교체 | `data/providers/price_provider.py` 의 `get_price_provider` |
| 색·카드 모양 | `components/ui.py` |
| 전술판 자리 배치·등번호·자동 정리 | `services/pitch_service.py` |
| 전술판 모양·캡처 이미지 | `components/football_pitch/frontend/index.html` |
| 유니폼 색 / 이름 축약 | `components/pitch_kit.py` |
| 스킨 추가·색 | `components/skins.py` (데이터만 추가하면 끝) |
| 스킨 해금 조건 | `services/skin_service.py` 의 `is_unlocked()` |
| 공유용 텍스트 | `services/share_service.py` |
| 화면 배치 | `app.py` |

---

## 배포 (Streamlit Community Cloud)

한 줄씩 지워 나가는 체크리스트는 **[DEPLOY.md](DEPLOY.md)** 에 있습니다.

1. 이 폴더를 GitHub 저장소로 push
2. share.streamlit.io 에서 저장소 연결, main 파일 `app.py`
3. 앱 URL 을 `MYETFPAYSTUB` 으로 지정

**빠뜨리면 앱이 아예 안 켜지는 것들**

- 새로 만든 파일(특히 `services/`, `components/`, `data/providers/issuer/`)을
  git 에 반드시 포함시킵니다. 빠지면 `ModuleNotFoundError` 입니다.
- `data/issuer_index.csv` 를 올려야 합니다. 없으면 ACE·RISE·SOL·PLUS·TIME 이
  통째로 폴백으로 내려가 과세표준이 전부 "데이터 없음" 이 됩니다.
- `.streamlit/config.toml` 을 올려야 합니다(밝은 테마 고정 — 안 올리면 다크모드
  기기에서 글자가 안 보입니다). `.streamlit/secrets.toml` 은 **절대 올리지 않습니다.**
- 새 서드파티 패키지를 쓰면 `requirements.txt` 에 직접 적습니다. 다른 패키지에
  딸려오는 것에 얹혀 가면 그쪽이 의존성을 빼는 순간 앱이 안 켜집니다.
- 배포 후 Streamlit Cloud 대시보드 Secrets 에 `COUNTER_NS` 를 넣습니다
  (방문자 카운터 네임스페이스. 공개 저장소에 적히면 남이 숫자를 올릴 수 있습니다).

---

## 아직 안 만든 것 (1차 버전 범위 밖)

AI 종목 추천 · 매수/매도 추천 · 종목 점수 · 시장 예측 · 뉴스 · 복잡한 차트 ·
리밸런싱 · 자동 주문 · 증권사 로그인/연동 · 종합소득세 신고 · 미래 세금 확정 계산 ·
최초 매수일 관리 · 과거 거래내역 관리 · 회원가입 · 결제.

나중에 붙일 수 있게 데이터 구조만 열어 두었습니다(실제 입금액 기록, 증권사 CSV
가져오기, 연간 금융소득 관리, 월별 자산 변화).
